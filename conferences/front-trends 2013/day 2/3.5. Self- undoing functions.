Self- undoing functions.

var restore = phase($("table tr:even > td:not(.bad)"));
// Sometime later..
restore();

function phase($el) {
    $el.hide();
    return function unphase() { $el.show(); }
} 